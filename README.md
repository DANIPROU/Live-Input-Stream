# Live-Input-Stream
I was bored and decided to make a small script that can read directly from the input and process each line individually. For now it does something really simple: computes the sum of the indexes of the letters in the latin alphabet. It also handles digit values.
